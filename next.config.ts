import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Do NOT set output: 'standalone' on Vercel — it manages output itself.
  // Standalone mode causes missing next-server.js.nft.json errors.
};

export default nextConfig;
